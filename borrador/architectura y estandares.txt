<!-- Ahí iremos escribiendo todas las decisiones técnicas.

Por ejemplo:

¿Por qué PySide6?
¿Por qué Excel?
¿Por qué una arquitectura por capas?
¿Por qué usamos servicios?

En empresas esto se conoce como un ADR (Architecture Decision Record). -->

1. Convención para nombres

Archivos:
excel_manager.py
main_window.py
dashboard_page.py
Siempre en snake_case.

Clases:
ExcelManager
MainWindow
DashboardPage
Siempre en PascalCase.

Funciones:
load_products()
create_excel()
save_config()
Siempre en snake_case.

Variables:
product_name
total_sale
current_stock
Siempre en snake_case.

Constantes:
APP_NAME
DEFAULT_THEME
APP_VERSION
Siempre en MAYÚSCULAS.

Vamos a usar Tipado.
Ejemplo:
def load_products() -> list:
en lugar de
def load_products():

Cada carpeta Python tendrá:
__init__.py
Aunque esté vacío. ¿Por qué? Porque convierte la carpeta en un paquete Python y facilita las importaciones.

Cada PR tendrá esta estructura
PR-001
│
├── Objetivo
├── Archivos nuevos
├── Código
├── Explicación
├── Checklist
└── Commit

FarmaciaPOS/
│
├── assets/
│   ├── fonts/
│   ├── icons/
│   ├── images/
│   ├── logos/
│   └── styles/
│       └── theme.qss
│
├── data/
│   ├── backups/
│   └── farmacia.xlsx
│
├── docs/
│
├── src/
│   │
│   ├── app/
│   │     ├── __init__.py
│   │     └── app.py
│   │
│   ├── core/
│   │     ├── __init__.py
│   │     ├── config.py
│   │     ├── constants.py
│   │     ├── logger.py
│   │     └── theme.py
│   │
│   ├── ui/
│   │     ├── __init__.py
│   │     ├── components/
│   │     ├── layout/
│   │     └── windows/
│   │
│   ├── modules/
│   ├── repositories/
│   ├── services/
│   └── models/
│
├── tests/
│
├── main.py
│
├── requirements.txt
│
├── README.md
│
└── CHANGELOG.md